using System;
//namespace in .NET framework 
using System.Collections.Generic;
//lists, collections, etc
using System.Linq;
//data
using System.Text;
//text, coding, etc
using System.Threading.Tasks;
//multithread applications

namespace HelloWorld
{
    class Program
    {
        static void Main(string[] args) //start point of application
            //paramaters (args) are optional
            //return type is void, means no value returned it's just code
            //case sensitive language, must be Main to be recognized
        {
            Console.WriteLine("Hello, World!");
                //method is purple cube
                //statements terminated by semi-colon

        }
    }
}